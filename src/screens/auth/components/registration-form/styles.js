import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  message: {
    marginTop: 30,
    textAlign: 'center',
    justifyContent: 'center',
    flexDirection: 'row'
  }
});

export { styles };
